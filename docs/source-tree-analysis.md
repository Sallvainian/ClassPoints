# Source Tree Analysis

_Generated 2026-04-28 (deep-scan rescan)._

ClassPoints is a single-part monolith — one React SPA backed by Supabase. The repo's interesting structure lives in `src/`, `supabase/`, `tests/`, `scripts/`, and `.github/workflows/`; everything else is config or generated output.

## Top-level layout

```
ClassPoints/
├── src/                        # React app source (97 .ts/.tsx files, excl. tests)
├── supabase/                   # Local Supabase config + 11 migrations
├── tests/                      # Playwright E2E + support helpers
├── scripts/                    # Node-side dev/CI/seed scripts (tsx + .mjs)
├── public/                     # Static assets served at /ClassPoints/
├── docs/                       # ← Generated by this workflow + ADRs + legacy inventories
├── _bmad/                      # BMAD installation (skills, scripts, config)
├── _bmad-output/               # PRD, architecture, project-context.md, audits
├── .github/
│   ├── workflows/              # 4 CI workflows
│   └── dependabot.yml
├── .agents/, .claude/, .auth/  # Agent / IDE / Playwright auth state
├── .air/                       # AIR (untracked; gitignored)
├── e2e.legacy/                 # Pre-current Playwright suite (kept for reference)
├── dist/                       # Production bundle (build output, gitignored)
├── node_modules/               # Dependencies
├── playwright-report/          # Last Playwright run output
├── test-results/               # Last Playwright artifacts
│
├── package.json                # Scripts + dependencies
├── package-lock.json           # ~389 KB lockfile
├── tsconfig.json + .app.json + .node.json   # Strict TS, isolatedModules, bundler resolution
├── vite.config.ts              # `base: '/ClassPoints/'`, react plugin, host: true
├── vitest.config.ts            # jsdom, src/test/setup.ts
├── playwright.config.ts        # Local-stack guard, .auth/user.json storage, Chromium-only
├── eslint.config.js            # Flat config (ESLint 9), TS + React-hooks + react-refresh
├── postcss.config.js           # @tailwindcss/postcss + autoprefixer
├── tailwind.config.js          # Tailwind v4 (minimal — most theme lives in src/index.css @theme)
├── .prettierrc                 # semi, single-quote, tabWidth 2, printWidth 100
├── fnox.toml                   # age-encrypted secrets (VITE_SUPABASE_*, VITE_TEST_*)
├── mise.toml                   # mise-managed tools (fnox, etc.)
├── .env.test (.example)        # Local-stack credentials for vite/playwright
├── .nvmrc                      # Node version
├── CLAUDE.md, AGENTS.md        # Project rules for AI agents
└── playwright-legacy-config.ts # Old Playwright config (alongside e2e.legacy/)
```

## `src/` — React application

```
src/
├── App.tsx                     # 4-provider stack: Auth > Theme > Sound > App. View routing via useState.
├── main.tsx                    # createRoot, QueryClientProvider, DevtoolsGate (DEV-only dynamic import)
├── index.css                   # Tailwind v4 @theme tokens (terracotta accent, Instrument Serif/Geist/JetBrains Mono),
│                               #   semantic surfaces/ink/hairline, .dark overrides, chart-grid vars
├── vite-env.d.ts
│
├── assets/
│   └── sounds/                 # SOUND_DEFINITIONS + synthesizeSound (Web Audio buffers, no .mp3 files)
│
├── components/                 # 46 .tsx (45 components + barrel index.ts files)
│   ├── auth/                   # AuthGuard, AuthPage, LoginForm, SignupForm, ForgotPasswordForm
│   ├── behaviors/              # BehaviorButton, BehaviorPicker
│   ├── classes/                # ImportStudentsModal
│   ├── common/                 # SyncStatus (realtime connection indicator)
│   ├── dashboard/              # DashboardView (in-class workflow), BottomToolbar
│   ├── home/                   # TeacherDashboard, ClassroomCard, LeaderboardCard, StatsCard
│   ├── layout/                 # Layout + Sidebar (editorial-redesign: terracotta dot + Instrument Serif title)
│   ├── migration/              # MigrationWizard (one-time localStorage→Supabase migration UI)
│   ├── points/                 # AwardPointsModal, ClassAwardModal, MultiAwardModal,
│   │                           #   ClassPointsBox (NEW — class total promoted to its own card),
│   │                           #   TodaySummary, UndoToast
│   ├── profile/                # ProfileView, DeleteClassroomModal
│   ├── seating/                # SeatingChartView, SeatingChartCanvas, SeatingChartEditor, SeatCard,
│   │                           #   TableGroup, RoomElementDisplay, EmptyChartPrompt, ViewModeToggle
│   ├── settings/               # ClassSettingsView, AdjustPointsModal, ResetPointsModal,
│   │                           #   SoundSettings, SoundSettingsModal
│   ├── students/               # StudentGrid, StudentPointCard
│   └── ui/                     # Button, Dialog, Input, Modal, ErrorToast (primitive design system)
│
├── contexts/                   # 4 React contexts (NOT migrated to TanStack — by design)
│   ├── AuthContext.tsx         # Supabase auth + stale-JWT graceful degrade (5s validate timeout, sb-* purge)
│   ├── AppContext.tsx          # 797 LOC. Thin wrapper around hooks. Phase 4 dissolution target.
│   ├── SoundContext.tsx        # Web Audio + user_sound_settings table + cross-device sync
│   └── ThemeContext.tsx        # light/dark, prefers-color-scheme + localStorage
│
├── hooks/                      # 13 hooks
│   ├── index.ts                # Barrel export (the only barrel — components do not re-export)
│   ├── useClassrooms.ts        # TanStack ✅ (Phase 2)
│   ├── useStudents.ts          # TanStack ✅ (Phase 3) — owns students realtime + point_transactions DELETE
│   ├── useTransactions.ts      # TanStack ✅ (Phase 2) — useAwardPoints is THE optimistic mutation showcase
│   ├── useBehaviors.ts         # TanStack ✅ (Phase 1)
│   ├── useLayoutPresets.ts     # ❌ Legacy hand-rolled (presets/loading/error/refetch shape) — DO NOT clone
│   ├── useSeatingChart.ts      # ❌ Legacy hand-rolled (1118 LOC, 23-value return) — DO NOT clone
│   ├── useRealtimeSubscription.ts # Generic WS subscription wrapper (crypto.randomUUID channel names)
│   ├── usePersistedState.ts    # Legacy localStorage state for migration wizard
│   ├── useDisplaySettings.ts   # Per-device UI prefs (cardSize, viewMode, showPointTotals)
│   ├── useAvatarColor.ts       # Theme-aware avatar bg + text color resolution
│   ├── useRotatingCategory.ts  # Generic auto-rotating category timer
│   └── useSoundEffects.ts      # AudioBuffer playback wrapper around SoundContext
│
├── lib/
│   ├── supabase.ts             # createClient<Database>; window.__SUPABASE_CLIENT__ for migration scripts
│   ├── queryClient.ts          # ADR-005 defaults: staleTime 30s, gcTime 10min, refetchOnWindowFocus false
│   ├── queryKeys.ts            # Single source of truth for query keys (use BOTH for read AND invalidate)
│   └── manualAdjustmentConstants.ts
│
├── services/
│   └── NetworkStatus.ts        # Online/offline subscription utility
│
├── types/
│   ├── database.ts             # Supabase-generated Database type + 30+ convenience aliases
│   ├── index.ts                # App-shape types (Behavior, Student, Classroom, AppState, UndoableAction)
│   ├── seatingChart.ts         # All seating chart types (DB + app + transforms colocated)
│   └── transforms.ts           # dbToBehavior/Classroom/Student/PointTransaction (boundary normalization)
│
└── utils/
    ├── index.ts                # Re-exports
    ├── dateUtils.ts            # getDateBoundaries (start of today, start of week)
    ├── defaults.ts             # Default behaviors + sounds
    ├── errorMessages.ts        # ERROR_MESSAGES dictionary
    ├── leaderboardCalculations.ts
    ├── migrateToSupabase.ts    # hasLocalStorageData, migration helpers (one-time wizard)
    ├── migrations.ts           # AppState schema migrations for legacy localStorage data
    ├── studentParser.ts        # Bulk-import name parser
    └── validateAudioUrl.ts     # Custom-sound URL validation + AudioBuffer load
```

## `supabase/` — Backend

```
supabase/
├── config.toml                 # Local-stack config (ports, auth, db version)
├── snippets/                   # SQL snippets (utility queries; not migrations)
└── migrations/
    ├── 001_initial_schema.sql              # classrooms, students, behaviors, point_transactions, RLS, default behaviors
    ├── 002_add_user_auth.sql               # user_id columns, user-scoped RLS, set_user_id() trigger
    ├── 003_sync_default_behaviors.sql      # 14 system-default behaviors (positive 8 + negative 6)
    ├── 004_enable_realtime.sql             # ALTER PUBLICATION supabase_realtime — point_transactions, classrooms, students, behaviors
    ├── 005_replica_identity_full.sql       # REPLICA IDENTITY FULL on point_transactions + students (DELETE payloads)
    ├── 006_add_batch_id.sql                # batch_id column for class-wide / multi-select award undo
    ├── 007_add_sound_settings.sql          # user_sound_settings (per-user prefs, realtime-enabled, REPLICA IDENTITY FULL)
    ├── 008_add_seating_charts.sql          # seating_charts (1:1 classroom), seating_groups, seating_seats, room_elements, layout_presets
    ├── 009_fix_room_element_dimensions.sql # Backfill + new defaults for teacher_desk / door
    ├── 010_add_room_element_types.sql      # ALTER TYPE room_element_type ADD VALUE — window, countertop, sink
    └── 011_add_student_point_totals.sql    # students.point_total/positive_total/negative_total + DB trigger + get_student_time_totals RPC
```

## `tests/` — E2E

```
tests/
├── README.md
├── e2e/
│   ├── auth.setup.ts           # Setup project: log in once → save .auth/user.json storage state
│   ├── example.spec.ts         # Smoke test
│   ├── global-setup.ts         # globalSetup: starts local Supabase + seeds test user
│   └── global-teardown.ts      # globalTeardown: stops local Supabase
└── support/
    ├── fixtures/
    │   ├── factories/user.factory.ts
    │   └── index.ts
    └── helpers/
        ├── auth.ts
        └── supabase-admin.ts
```

There's also a `src/test/` directory with Vitest unit tests:

```
src/test/
├── setup.ts                    # Vitest setup (testing-library/jest-dom)
├── leaderboardCalculations.test.ts
├── sounds.test.ts
├── TeacherDashboard.test.tsx
└── useRotatingCategory.test.ts

src/hooks/__tests__/
└── useRealtimeSubscription.test.ts

src/utils/__tests__/
└── studentParser.test.ts
```

## `scripts/` — Node-side dev/CI/seed scripts

```
scripts/
├── dev.mjs                     # `npm run dev` entrypoint — local-by-default; preflights Docker, manages Supabase lifecycle
├── lib/
│   └── supabase-host.mjs       # readEnvTest, shouldManageLocalStack, ensureDockerRunning, isStackRunning
├── check-bundle.mjs            # CI: assert prod bundle has no react-query-devtools
├── seed-test-user.ts           # Seeds .env.test user via service role key
├── seed-test-classroom.ts      # Sample classroom data
├── seed-counter-data.ts        # Sample point counter data
├── browser-migrate.js          # Browser-console migration runner
├── migrate-data.ts             # Tooling for hosted migrations
├── ci-local.sh                 # Local CI dry-run
├── burn-in.sh                  # Manual burn-in test loop
├── test-changed.sh             # Run tests for changed files only
└── verify-undo-fix.ts          # Ad-hoc verification (gitignored variants — verify-*.ts)
```

## `.github/workflows/` — CI

```
.github/
├── dependabot.yml
└── workflows/
    ├── test.yml                # Test pipeline: lint → bundle-check → 4-shard E2E → burn-in → summary
    ├── deploy.yml              # Deploy to GitHub Pages (build with fnox + lint + typecheck + unit tests)
    ├── claude.yml              # Claude Code action
    └── claude-code-review.yml  # Claude PR review action
```

## `docs/` — This workflow's output

```
docs/
├── index.md                    # ← Master entry point (start here)
├── project-overview.md
├── architecture.md
├── source-tree-analysis.md     # ← This file
├── data-models.md
├── state-management.md
├── component-inventory.md
├── development-guide.md
├── modernization-plan.md       # Hand-written; describes target architecture
├── point-counter-inventory.md  # Hand-written; counter-feature inventory
├── rules-review-category-3.md  # Hand-written; ruleset audit
├── project-scan-report.json    # State file for this workflow (resume / progress)
│
├── adr/
│   └── ADR-005-queryclient-defaults.md     # Authoritative; §1-§6 all in force
│
├── legacy/                     # AS-IS pattern inventory; legacy-migrations / legacy-testing / legacy-utils still authoritative,
│                               # the rest describe patterns being reversed during the TanStack migration
│   ├── legacy-components.md
│   ├── legacy-contexts.md
│   ├── legacy-hooks.md
│   ├── legacy-migrations.md
│   ├── legacy-state-management.md
│   ├── legacy-supabase.md
│   ├── legacy-testing.md
│   └── legacy-utils.md
│
├── screenshots/
└── .archive/                   # Older project-scan-report.json snapshots
```

## Critical entry points

| Path                          | What it is                                                                                                                   |
| ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `src/main.tsx`                | App bootstrap. Mounts `<App />` inside `QueryClientProvider`; registers DevtoolsGate (DEV-only).                             |
| `src/App.tsx`                 | Provider stack + view router (home / dashboard / settings / migration / profile). View persists via `localStorage:app:view`. |
| `src/contexts/AppContext.tsx` | `useApp()` legacy facade — Phase 4 dissolution target. New components MUST call hooks directly.                              |
| `src/lib/supabase.ts`         | Single Supabase client. `window.__SUPABASE_CLIENT__` for migration scripts.                                                  |
| `src/lib/queryClient.ts`      | TanStack QueryClient with ADR-005 defaults (load-bearing for the optimistic-mutation Phases 1–3).                            |
| `src/lib/queryKeys.ts`        | Single source of truth for query keys — used for both reads AND invalidations to keep them in sync.                          |
| `scripts/dev.mjs`             | `npm run dev` entrypoint. Local-by-default; starts/stops local Supabase + preflights Docker.                                 |
| `playwright.config.ts`        | E2E. Fail-closed network allow-list (loopback, RFC1918, Tailscale CGNAT).                                                    |
| `supabase/migrations/`        | 11 migrations in numerical order.                                                                                            |

## Where to add new code

| What                   | Where                                                                                                                                                                                        |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| New TanStack data hook | `src/hooks/useX.ts` + add to `src/hooks/index.ts` barrel. Use `useStudents` / `useTransactions` as templates.                                                                                |
| New component          | `src/components/<feature>/X.tsx` + a sibling `index.ts` barrel re-exporting it. **Named exports only.**                                                                                      |
| New DB column          | SQL migration → `database.ts` `DbX`/`NewX`/`UpdateX` → if user-facing, `index.ts` app type → `transforms.ts` (skip if `dbToPointTransaction`-style passthrough). Verify `.select()` clauses. |
| New utility            | Cross-feature → `src/utils/X.ts`. Feature-scoped → sibling `*.utils.ts`.                                                                                                                     |
| New types              | App types → `src/types/index.ts`. Feature-scoped → sibling `*.types.ts`.                                                                                                                     |
| Cross-component helper | `src/utils/`. Component files export ONLY components (HMR predictability).                                                                                                                   |
