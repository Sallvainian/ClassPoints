# BMM Workflow Status - ClassPoints
# Generated by workflow-init
# Track: BMad Method (Brownfield)

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#   - skipped: Optional workflow that was explicitly skipped
#
# Completion Status:
#   - {file-path}: File created/found (replaces initial status)

generated: "2025-12-12"
project: "ClassPoints"
project_type: "web-app"
selected_track: "method"
field_type: "brownfield"
workflow_path: "method-brownfield.yaml"

# Discovery workflows (user selections)
discovery:
  brainstorm_requested: false
  research_requested: false
  product_brief_requested: false

# Workflow status by phase
workflow_status:
  # Prerequisite: Documentation (for brownfield projects)
  prerequisite:
    document-project:
      status: "docs/index.md"
      completed: "2025-12-12"
      agent: "analyst"
      command: "/bmad:bmm:workflows:document-project"
      purpose: "Comprehensive codebase analysis for AI agent context"
      note: "Exhaustive scan completed - 29 components, 9 hooks, 7 docs generated"

  # Phase 1: Planning
  phase_1_planning:
    create-prd:
      status: "required"
      agent: "pm"
      command: "/bmad:bmm:workflows:create-prd"
      purpose: "Define product requirements for new features/changes"

    create-ux-design:
      status: "conditional"
      condition: "if_has_ui"
      agent: "ux-designer"
      command: "/bmad:bmm:workflows:create-ux-design"
      purpose: "Design user experience for UI changes"

  # Phase 2: Solutioning
  phase_2_solutioning:
    create-architecture:
      status: "recommended"
      agent: "architect"
      command: "/bmad:bmm:workflows:create-architecture"
      purpose: "Integration architecture for new features"
      note: "HIGHLY RECOMMENDED: Distills brownfield context into focused solution design"

    create-epics-and-stories:
      status: "required"
      agent: "pm"
      command: "/bmad:bmm:workflows:create-epics-stories"
      purpose: "Break PRD into implementable epics and stories"
      depends_on: ["create-prd"]

    test-design:
      status: "recommended"
      agent: "tea"
      command: "/bmad:bmm:workflows:testarch-test-design"
      purpose: "System-level testability review"

    implementation-readiness:
      status: "required"
      agent: "architect"
      command: "/bmad:bmm:workflows:check-implementation-readiness"
      purpose: "Validate PRD + Architecture + Epics alignment"
      depends_on: ["create-prd", "create-architecture", "create-epics-and-stories"]

  # Phase 3: Implementation
  phase_3_implementation:
    sprint-planning:
      status: "required"
      agent: "sm"
      command: "/bmad:bmm:workflows:sprint-planning"
      purpose: "Create sprint plan with prioritized stories"
      depends_on: ["implementation-readiness"]
      note: "Creates sprint-status.yaml for implementation tracking"

# Next action
next_workflow:
  id: "create-prd"
  name: "Create PRD"
  agent: "pm"
  command: "/bmad:bmm:workflows:create-prd"
  reason: "Define product requirements for new features/changes"
