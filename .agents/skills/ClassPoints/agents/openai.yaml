interface:
  display_name: "ClassPoints"
  short_description: "Repo-specific patterns and workflows for ClassPoints"
  default_prompt: "Use the ClassPoints repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true