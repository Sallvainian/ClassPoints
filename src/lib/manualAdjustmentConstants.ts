// Display fields written to `point_transactions` rows that represent a manual
// teacher-driven point adjustment (no template behavior). Shared between the
// AppContext wrapper (for backward-compat imports) and `useAdjustStudentPoints`.
export const MANUAL_ADJUSTMENT_NAME = 'Manual Adjustment';
export const MANUAL_ADJUSTMENT_ICON = '✏️';
