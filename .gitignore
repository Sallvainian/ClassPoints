# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files
# Secrets live in fnox.toml (age-encrypted, safe to commit).
# .env.test holds local Supabase credentials for E2E — ignored, use .env.test.example as a template.
.env.test
.env.local

# Supabase CLI transient state
supabase/.temp/

# AI tooling folders (local configs, not project code)
.agent/
.agents/
.air/
.bmad/
.codex/
.claude/*
!.claude/rules/
.cursor/
.mcp.json
.serena/

# Test artifacts
playwright-report/
test-results/
.playwright-mcp/
.playwright-cli/
_bmad/
# BMAD output_folder (see _bmad/*/config.yaml output_folder) — generated
# planning / spec / traceability / gate artifacts, regenerated every run.
_bmad-output/
# BMad document-project state-file snapshots — kept on local disk, never tracked.
docs/.archive/
.auth/
src.zip
# Skill-generated scratch (playwright-test / BMAD TEA gate outputs)
skills/

# Local one-off verification scripts (kept on disk but not tracked)
scripts/verify-*.ts
.dmux/
.coddo/
.claude/
.toddo/
