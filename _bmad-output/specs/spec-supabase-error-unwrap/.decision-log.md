# Decision Log — spec-supabase-error-unwrap

## 2026-05-31 — Create (headless distill from deferred-work.md)

Distilled from `deferred-work.md` entry #14 (`:116-122`) and the Tier-3 framing (`:213`). No PRD FR maps to this item — it originated in the project-context refresh backlog — so `deferred-work.md` is the sole source.

**Pass 1 (Coherence).** Three capabilities, each intent + testable success (helper exists + unit-tested, no hand-rolled `if (error)` in migrated hooks, metadata survives). Constraints bend design: incremental migration (not big-bang) and metadata preservation (not `error.message` flattening). Non-goals protect the existing code-discrimination contract and forbid a one-shot rewrite. Success signal is demonstrable via unit tests + a `SoundContext`-style discrimination check.

**Pass 2 (Preservation).** All load-bearing claims from the spec seed (`:121`) and acceptance (`:122`) landed: `unwrap<T>()` in `src/lib/supabase.ts`, incremental hook migration, preserve original error metadata, keep `SoundContext`-style code discrimination working, unit-test the helper. No companion needed. Wrapper-only content dropped: none.
