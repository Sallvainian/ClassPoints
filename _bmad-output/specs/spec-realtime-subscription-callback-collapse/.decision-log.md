# Decision Log — spec-realtime-subscription-callback-collapse

## 2026-05-31 — Create (headless distill from deferred-work.md + prd.md)

Distilled from `deferred-work.md` entry #13 (`:108-114`) and the Tier-3 sequencing note (`:212`), grounded against `prd.md` FR8 (`:434`) and FR20 (`:461`).

**Pass 1 (Coherence).** Three capabilities, each intent + testable success (two `rg` invariants + a test-coverage criterion). The sequencing dependency is a real design-bender, so it is a Constraint, not a footnote: #13 cannot remove the legacy props until #11 lands and the `useStudents` DELETE branch converts. Non-goals fence off transport changes and the #11 prerequisite itself. Success signal is demonstrable via type-signature + grep + routing tests.

**Pass 2 (Preservation).** All load-bearing claims from the spec seed (`:113`) and acceptance (`:114`) landed: convert legacy callers, remove props + dev warning, tests cover INSERT/UPDATE/DELETE via `onChange`. The "only two legacy callers" fact (`:212`) is preserved in the Constraint so the implementer knows the exact migration set. No companion needed. Wrapper-only content dropped: none.
