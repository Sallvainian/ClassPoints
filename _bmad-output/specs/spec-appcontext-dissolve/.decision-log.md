# Decision Log — spec-appcontext-dissolve

## 2026-05-31 — Create (headless distill from prd.md + deferred-work.md)

Distilled from `prd.md` "Phase 4 — Slim AppContext" (`:372`+) and FR13/FR14/FR15 (`:448-450`), reconciled against the 2026-05-31 migration-completion synthesis in `deferred-work.md` (entries #10, :194-203).

**Conflicts resolved.** Two PRD numbers were superseded by the later synthesis, which was written to correct the PRD:

- **45 → 8 consumers.** `prd.md:374` says "the 45 component files"; `deferred-work.md:198` is an explicit scope correction ("Actual `useApp()` UI consumers = 8") and names all eight. Used 8; flagged the PRD figure as stale so it cannot reset the scope.
- Kept the undo-window machinery (`getRecentUndoableAction`/`batchKindRef`) as an explicit Capability (CAP-4) rather than letting downstream treat it as deletable adapter — `deferred-work.md:202` calls it the irreducible UI/session core.

**Pass 1 (Coherence).** Five capabilities, each with intent + testable success (line-count, import-grep, behavior-identity checks). Constraints bend design: behavior-preserving migration, separate-commit rule for behavior changes, the 8-not-45 scope, selector relocation, no-new-fields. Non-goals fence off #1/#6/#7 and the seating/layout reshapes. Success signal is demonstrable (new-feature-without-editing-AppContext + full walkthrough parity). One open question (undo-machinery home) left for the architecture phase per source.

**Pass 2 (Preservation).** All load-bearing claims from `prd.md:372-386` (scope, <200 LOC, zero hook imports, UI-only return, adapter deletion, tests-pass, smoke walkthrough) and FR13-15 landed in SPEC.md. The catalog content (8 consumers, 6 fields, ~20 wrappers, 4 selectors, undo machinery) moved to companion `appcontext-surface.md` rather than bloating the kernel. `project-context.md` was NOT cited (not read this session); its "MUST-NOT-add-fields" rule is carried via `deferred-work.md:196` + FR4 instead. Wrapper-only content dropped: none.

**Undo-machinery open question resolved (user, 2026-05-31).** Decision: extract `getRecentUndoableAction` + `batchKindRef` to a dedicated `useUndoableAction` hook — do NOT leave it in `AppContext`. Rationale (user): it's consumed by exactly one component (`DashboardView`), so a focused hook is the natural home; leaving "exceptions" in `AppContext` undercuts Phase 4's whole point of stopping it being a catch-all; and the extracted hook is a clean seam to later fix #6 (1Hz poll) and #7 (`batch_kind`) without touching the context. CAP-4, the Non-goal line, and companion `appcontext-surface.md` were updated; the open question was removed (Open Questions section dropped, Assumptions retained).

**Grounding correction (post-write).** The companion's per-component consumption column was initially written from inference — `deferred-work.md:198` supplies only the 8 component _names_, not what each consumes. That column was a fabrication and was rejected before shipping. The eight files were then read directly (`rg -B 18 '= useApp()'`) and the column was rewritten verbatim from each call site's destructuring with `path:line` citations. The inferred version had errors the grounded pass corrected — e.g. `MultiAwardModal` consumes `behaviors`/`awardPointsToStudents` (not "students"), and `ProfileView` consumes `classrooms`/`deleteClassroom`/`activeClassroomId`/`setActiveClassroom` (not vague "user-scoped data"). Grounded reading also sharpened CAP-2: the only true UI/session fields in the surface are `activeClassroomId` + `setActiveClassroom`; `activeClassroom` (resolved object) is server-derived and must become a cache read.
