# Decision Log — spec-seating-chart-migration

## 2026-05-31 — Create (headless distill from prd.md + deferred-work.md)

Distilled from `prd.md` "Phase 5 — `useSeatingChart` Split" (`:388-401`) and FR16/FR17/FR18 (`:454-456`), cross-referenced with `deferred-work.md` entry #12 (`:100-106`, `:208`) and the seating-realtime WONTFIX (`:24-27`).

**Conflicts resolved.**

- **Rollback count 4 vs 5.** `prd.md:397` says "the four rollback-capture sites"; `deferred-work.md:189` says "5 manual rollback sites (`:340,526,669,813,871`)." Rather than pick a number, CAP-2 / success signal use the invariant phrasing the PRD itself supplies at `:396` — "zero manual `const previous = ...` rollback captures for server state." The count and line numbers become non-binding implementation hints, so the contract never depends on a figure that needs `useSeatingChart.ts` opened to confirm.
- **Stale smoke case.** `prd.md:401` lists "drag a seat that a realtime event arrives for mid-drag," which contradicts the no-realtime decision at `prd.md:399` and the obsolete FR18 (`:456`). Logged as an open question to drop that smoke case, not as a capability.

**Pass 1 (Coherence).** Four capabilities, each intent + greppable/testable success. Constraints bend design hard: no realtime (rules out the entire mid-drag-sync design space), UI-layer choice deferred, tests unchanged. Non-goals fence off mid-drag realtime, cross-device sync, and non-seating hooks. Success signal is demonstrable via the drag/cancel/save-preset smoke set plus a grep invariant.

**Pass 2 (Preservation).** All load-bearing claims from `prd.md:388-401` and FR16/FR17 landed; FR18's obsolescence is preserved as a Non-goal + Constraint. The four-table enumeration (`seats`, `seating_groups`, `room_elements`, layout presets) from `prd.md:390` is in CAP-1/CAP-4. No companion needed — content fit the kernel. Wrapper-only content dropped: the PRD's risk-narrative prose ("Hardest single piece of work") is folded into Why, not preserved verbatim.

## 2026-05-31 — Update (open question resolved by user)

User confirmed seating-chart is not a realtime domain. This resolves the open question that flagged the contradictory "drag a seat that a realtime event arrives for mid-drag" smoke case in `prd.md:401`: that smoke case is dropped. The open question was removed from `SPEC.md`; the no-realtime contract (CAP-4 + Constraint + Non-goal) was already correct and is unchanged. Source-side action still pending offer: the stale `prd.md:401` smoke bullet has not been edited (PRD left as-is per scope).

**UI-state-layer open question resolved (user, 2026-05-31).** Decision: local component `useState`, with `useRef` for high-frequency pointer-move values during an active drag. **No Zustand.** Rationale (user): a single drag canvas needs no cross-tree state sharing, and Zustand would be a new dependency the rest of the app doesn't use; revisit a seating-scoped store only if drag/hover/selection must be read outside the canvas subtree. CAP-3 and the UI-state Constraint were updated to encode the decision; the open question was removed (section now empty → omitted).
