# Decision Log — spec-layout-presets-migration

## 2026-05-31 — Create (headless distill from deferred-work.md + prd.md)

Distilled from `deferred-work.md` entry #11 (`:92-98`) and the Tier-2 framing (`:207`), grounded against `prd.md` FR9 (`:438`) for the non-realtime-domain rule and Phase 2's acceptance criteria (`:348`).

**Pass 1 (Coherence).** Three capabilities, each intent + testable success (zero-useState grep, no-realtime grep, consumer compile + invalidation tests). Constraints bend design: realtime is deleted not migrated, typecheck must pass. Non-goal explicitly hands the legacy-callback collapse to #13. Success signal is demonstrable (grep + two-tab save/load smoke).

**Pass 2 (Preservation).** All load-bearing claims from the spec seed (`deferred-work.md:97`) and acceptance (`:98`) landed: `useQuery` + split mutations keyed by `queryKeys.layoutPresets.all`, remove realtime, no `useState` state, consumers compile, tests cover list/load + invalidation. The verified-status detail (`:186` — `useLayoutPresets.ts:23-25` 3×useState, `:56-75` legacy realtime) is folded into Why as non-binding context. No companion needed. Wrapper-only content dropped: none.
